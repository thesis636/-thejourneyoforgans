import React from "react";

const Scene77 = React.forwardRef((props, ref) => {
  return (
    <p ref={ref} className="centered-paragraph text-black grid place-items-center h-full" style={{ opacity: 0 }}>
      นี่เป็นเพียงแค่ผู้คนส่วนหนึ่ง
      <br />
      ที่ฉันพาเธอมาดูเท่านั้น
    </p>
  );
});

export default Scene77;
