import React from "react";

const Scene68 = React.forwardRef((props, ref) => {
  return (
    <p ref={ref} className="centered-paragraph top-[13%]" style={{ opacity: 0 }}>
      เธอเห็นเด็กผู้ชายที่ใส่
      <br />
      ชุดตัวสำรองตรงนั้นมั้ย?
    </p>
  );
});

export default Scene68;
